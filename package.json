{
  "name": "zajnotesttask",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "cleanup": "rm -rf .cache dist docs",
    "dev": "npm run cleanup && parcel src/index.html --public-url ./",
    "build": "npm run cleanup && parcel build src/index.html --public-url ./ && xcopy dist docs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.12.5",
    "sass": "^1.45.0"
  }
}
